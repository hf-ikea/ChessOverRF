namespace Game{
    public class Player
    {
        public string callsign;
        public bool opponentTurn;
        public Player(string callsign)
        {
            this.callsign = callsign;
        }
    }
}